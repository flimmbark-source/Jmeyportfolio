---
const { items = [] } = Astro.props;
---
{items.length > 0 && (
  <ul class="mt-8 space-y-4">
    {items.map((item) => (
      <li class="flex items-start gap-3 rounded-xl border border-emerald-100 bg-emerald-50/80 p-4 text-emerald-800 transition-colors duration-300 dark:border-emerald-400/40 dark:bg-emerald-500/20 dark:text-emerald-200">
        <span class="mt-1 inline-flex h-6 w-6 items-center justify-center rounded-full bg-emerald-500 text-sm font-bold text-white dark:bg-emerald-400">
          ✓
        </span>
        <p class="text-base leading-relaxed">{item}</p>
      </li>
    ))}
  </ul>
)}
